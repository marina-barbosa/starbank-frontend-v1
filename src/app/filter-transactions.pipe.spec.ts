import { FilterTransactionsPipe } from './filter-transactions.pipe';

describe('FilterTransactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTransactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
